/*
 * 这个文件做什么：预留首页占位（当前不使用，根路由会跳转到 /dashboard）。
 * What this file is for: placeholder home page (currently unused; '/' redirects to /dashboard).
 */

export default function Home() {
  return <div className="p-6 text-sm text-zinc-600">Home (unused)</div>;
}
