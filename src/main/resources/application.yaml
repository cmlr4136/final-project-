ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

db:
  url: "jdbc:h2:file:./data/fitness;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE"
  driver: "org.h2.Driver"
  user: "sa"
  password: ""

auth:
  tokenTtlHours: 168

seed:
  adminEmail: "admin@example.com"
  adminPassword: "admin"
